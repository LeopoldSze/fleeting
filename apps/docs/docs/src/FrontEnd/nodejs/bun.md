# Bun

特点：

- 是一个基于 `JSCore` 的js运行时环境
- 自带包管理器
- 自带转译器，天生支持ts等
- 自带测试运行器，不用安装 `jest、vitest` 等
- 自带构建构建
- 自带文件热更新，如命令 `--watch`，不用再使用 `nodemon` 等工具
- ESM 与 CommonJS格式兼容
- 内置浏览器API，如 `fetch` 等

**基于Bun的框架对比：**

- `Elysia`：最快
- `Hono`：
- `Baojs`：
