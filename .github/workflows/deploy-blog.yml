name: Deploy Blog to Vercel

on:
  push:
    branches:
      - main
    paths:
      - 'apps/blog/**'
      - .github/workflows/deploy-blog.yml
  pull_request:
    branches:
      - main
    paths:
      - 'apps/blog/**'
      - .github/workflows/deploy-blog.yml
  workflow_dispatch: {}

concurrency:
  group: deploy-blog-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy apps/blog to Vercel (prod)
    runs-on: ubuntu-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build blog
        run: pnpm build:blog

      - name: Vercel Pull
        run: pnpm dlx vercel pull --yes --environment=production --token "$VERCEL_TOKEN" --cwd apps/blog

      - name: Vercel Build
        run: pnpm dlx vercel build --prod --token "$VERCEL_TOKEN" --cwd apps/blog

      - name: Vercel Deploy (prebuilt)
        run: pnpm dlx vercel deploy --prebuilt --prod --token "$VERCEL_TOKEN" --cwd apps/blog
